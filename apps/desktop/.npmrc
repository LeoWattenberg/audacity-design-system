# pnpm's symlinked node_modules layout breaks Electron's require('electron')
# resolution — the built-in module path that Electron expects collides with
# the symlinked npm wrapper package, leaving `app` undefined at runtime.
# Hoisting the desktop package's modules fixes the resolution. Only affects
# this app (sandbox + packages stay on the default symlinked layout).
node-linker=hoisted
